---
title: "Installing CodeVibe"
description: "Choose your installation path: IDE Extension, CLI, SDK, or Kanban"
---

## Choose Your Install Path

- [IDE Extension](#ide-extension) — VS Code, Cursor, JetBrains, Windsurf, VSCodium, Antigravity
- [CLI](#cli) — terminal workflows
- [Kanban](#kanban) (preview) — easily manage through multiple agents through a kanban board
- [SDK](#sdk) — build with the CodeVibe runtime

## IDE Extension

Use this if you want CodeVibe inside your editor UI.

<Tabs>
  <Tab title="VS Code / Cursor / Windsurf / VSCodium / Antigravity">
    <Steps>
      <Step title="Open Extensions">
        Press `Ctrl/Cmd + Shift + X`.
      </Step>
      <Step title="Install CodeVibe">
        Install the latest CodeVibe VSIX from [GitHub Releases](https://github.com/atnumridha/codevibe/releases/latest), or install the local VSIX generated by `npm run package:github-vsix`.
      </Step>
      <Step title="Open CodeVibe">
        Run `CodeVibe: Open CodeVibe` from Command Palette.
      </Step>
      <Step title="Authorize with Codex">
        CodeVibe reads `~/.codex/auth.json` by default when available. You can also complete provider setup in CodeVibe settings.

        [Authorize with CodeVibe](/getting-started/authorization)
      </Step>
    </Steps>

    <Note>
    Windsurf and VSCodium use Open VSX. The install flow is the same.
    </Note>
  </Tab>

  <Tab title="JetBrains">
    <Steps>
      <Step title="Open Plugins Marketplace">
        **Settings** → **Plugins** → **Marketplace**.
      </Step>
      <Step title="Install CodeVibe-compatible plugin">
        JetBrains support remains on the upstream-compatible plugin track while CodeVibe focuses on VS Code, CLI, and standalone UI.
      </Step>
      <Step title="Open the agent panel">
        Open the installed agent panel from the IDE tool windows menu.
      </Step>
      <Step title="Authorize">
        Complete provider setup in the agent settings.

        [Authorize with CodeVibe](/getting-started/authorization)
      </Step>
    </Steps>

    Alternative: install the upstream-compatible plugin from the JetBrains Marketplace.
  </Tab>
</Tabs>

## CLI

Use this if you want CodeVibe in terminal workflows (interactive + automation).

<Steps>
  <Step title="Install Node.js">
    Install Node.js 20+ (22 recommended).
  </Step>
  <Step title="Install CLI">
    ```bash
    npm install -g codevibe
    ```
  </Step>
  <Step title="Authenticate">
    ```bash
    codevibe auth
    ```
  </Step>
  <Step title="Run CodeVibe">
    ```bash
    codevibe
    # or
    codevibe "your task"
    ```
  </Step>
</Steps>

More details: [CLI Installation & Setup](/usage/cli-overview)

## Kanban

Use this if you want task-board workflows with agent execution.

<Steps>
  <Step title="Install Node.js">
    Install Node.js 18+.
  </Step>
  <Step title="Launch Kanban">
    ```bash
    codevibe kanban
    ```
  </Step>
</Steps>

More details: [Kanban](/usage/kanban)

## SDK

Use this if you are building your own app or agent on top of the CodeVibe runtime.

<Steps>
  <Step title="Create project">
    ```bash
    mkdir my-agent && cd my-agent
    npm init -y
    ```
  </Step>
  <Step title="Install SDK">
    Follow the SDK examples for the currently published runtime package and import surface.
  </Step>
  <Step title="Build and run">
    Browse SDK examples to run your first agent.
  </Step>
</Steps>

Start here: [SDK Examples](/sdk/examples)


## Need Help?

- [Troubleshooting](/troubleshooting/networking-and-proxies)
- [GitHub Discussions](https://github.com/atnumridha/codevibe/discussions)
