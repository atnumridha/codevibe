---
title: "Authorization"
description: "Authenticate with Codie and choose your first AI model"
---

Codie connects to AI models through a **provider**. You can use:

- **Codie provider** (default): use the configured Codie sign-in and model path.
- **Local auth import**: reuse `~/.codex/auth.json` when compatibility support is enabled.
- **Codie account provider**: sign in with a supported hosted account flow when configured.
- **Bring Your Own Key (BYOK)**: use your own provider credentials (cloud or local runtimes).

## Menu

- [IDE Setup](#ide-setup)
- [CLI Setup](#cli-setup)

## IDE Setup

<Steps>
  <Step title="Open Codie Settings">
    Click the settings icon in the Codie panel.
  </Step>

  <Step title="Select Provider">
    Choose your desired provider from the **API Provider** dropdown.
  </Step>

  <Step title="Authenticate">
    - **Codie provider:** complete Codie sign-in from the provider settings.
    - **Local auth import:** keep `~/.codex/auth.json` populated when compatibility support is enabled.
    - **Codie account provider:** click **Sign In** and complete OAuth.
    - **BYOK cloud provider:** Paste your API key into the **API Key** field.
    - **Local runtime (Ollama/LM Studio):** no key needed; ensure runtime is running.
  </Step>

  <Step title="Select Model">
    Choose a recommended, cloud, or local model from the **Model** dropdown.
  </Step>
</Steps>

## Provider Options

### Local Auth Import

- Reads `~/.codex/auth.json`, `~/.codex/installation_id`, and local model cache when compatibility support is enabled
- Can use `openai-codex` internally for compatible profiles without making it the visible product path
- Preserves session headers and refresh behavior without logging token contents

### Codie Account Provider

- One sign-in when a hosted account backend is configured
- Unified model selection from the Codie settings UI
- Legacy provider IDs may remain internally so existing profiles continue to load

### BYOK (cloud + local)

### Cloud Providers

| Provider | Best For | Setup Guide |
|----------|----------|-------------|
| **Multi-model router** | Broad hosted model access | [Setup](/provider-config/openrouter) |
| **Direct hosted provider** | First-party model APIs | [Setup](/provider-config/openai) |
| **Enterprise cloud** | Managed cloud model access | [Setup](/provider-config/aws-bedrock/api-key) |
| **DeepSeek** | DeepSeek API access | [Setup](/provider-config/deepseek) |

### Local Models

Run models on your own hardware for local inference.

| Provider | Best For | Setup Guide |
|----------|----------|-------------|
| **Ollama** | CLI-based local runtime | [Setup](/running-models-locally/overview#runtime-options) |
| **LM Studio** | GUI-based local runtime | [Setup](/running-models-locally/overview#runtime-options) |

Local models require sufficient hardware (especially GPU memory). See [Running Models Locally](/running-models-locally/overview) for requirements.

## CLI Setup

```bash
# Authenticate from the terminal
codevibe auth

# Shorthand
codevibe a
```

Runs the same auth flow as IDE setup.

## Troubleshooting

| Issue | Fix |
|-------|-----|
| "Unauthorized: Please sign in" | Session expired. Click **Sign In** to re-authenticate. |
| Browser doesn't open | Check default browser settings. Copy the URL from the Codie output panel manually. |
| Frequent re-authentication | Check org security policies. Ensure you're not clearing IDE secrets. Try a full sign-out/sign-in. |
| Can't access organization | Ask your workspace admin about permissions. Sign out and back in. |
