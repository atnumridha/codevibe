name: ext-vscode-github-release

on:
    workflow_dispatch:
        inputs:
            tag:
                description: "Release tag to create or reuse, e.g. v3.1.2"
                required: true
                type: string
            create_tag_from_ref:
                description: "Create the tag from the dispatched ref if it does not exist"
                required: true
                default: true
                type: boolean
            prerelease:
                description: "Mark the GitHub Release as a prerelease"
                required: true
                default: true
                type: boolean
            draft:
                description: "Create the GitHub Release as a draft"
                required: true
                default: false
                type: boolean
            run_tests:
                description: "Run the VS Code extension test workflow before packaging"
                required: true
                default: true
                type: boolean

permissions:
    contents: write
    checks: write
    pull-requests: write

jobs:
    test:
        if: ${{ github.event.inputs.run_tests == 'true' }}
        uses: ./.github/workflows/ext-vscode-test.yml

    package-release:
        name: Package VSIX and Publish GitHub Release
        runs-on: ubuntu-latest
        needs: [test]
        if: ${{ always() && (github.event.inputs.run_tests != 'true' || needs.test.result == 'success') }}
        defaults:
            run:
                working-directory: apps/vscode

        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
                  fetch-tags: true
                  lfs: true

            - name: Resolve Release Tag
              id: resolve_tag
              working-directory: ${{ github.workspace }}
              env:
                  TAG: ${{ github.event.inputs.tag }}
                  CREATE_TAG: ${{ github.event.inputs.create_tag_from_ref }}
              run: |
                  if [[ -z "$TAG" ]]; then
                    echo "Error: tag input is required"
                    exit 1
                  fi

                  if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.]+)?$ ]]; then
                    echo "Error: tag must match vX.Y.Z, optionally with a suffix"
                    exit 1
                  fi

                  git fetch origin --tags
                  TAG_REF="refs/tags/$TAG"

                  if git show-ref --verify --quiet "$TAG_REF"; then
                    git checkout --detach "$TAG_REF^{commit}"
                  elif [[ "$CREATE_TAG" == "true" ]]; then
                    git config user.name "github-actions[bot]"
                    git config user.email "github-actions[bot]@users.noreply.github.com"
                    git tag "$TAG" "$GITHUB_SHA"
                    git push origin "$TAG_REF"
                    git checkout --detach "$TAG_REF^{commit}"
                  else
                    echo "Error: tag '$TAG' does not exist and create_tag_from_ref is false"
                    exit 1
                  fi

                  echo "tag=$TAG" >> "$GITHUB_OUTPUT"
                  echo "resolved_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: Install extension dependencies
              working-directory: ${{ github.workspace }}
              run: npm --prefix apps/vscode install --include=optional

            - name: Install webview-ui dependencies
              working-directory: ${{ github.workspace }}
              run: npm --prefix apps/vscode/webview-ui install --include=optional

            - name: Install VSIX packaging tool
              run: npm install -g @vscode/vsce

            - name: Get Version
              id: get_version
              run: |
                  VERSION=$(node -p "require('./package.json').version")
                  echo "version=$VERSION" >> "$GITHUB_OUTPUT"

            - name: Verify Tag Matches Package Version
              run: |
                  TAG="${{ steps.resolve_tag.outputs.tag }}"
                  VERSION="v${{ steps.get_version.outputs.version }}"
                  if [[ "$TAG" != "$VERSION" ]]; then
                    echo "Error: tag '$TAG' does not match package version '$VERSION'"
                    exit 1
                  fi
                  echo "Tag and package version match: $TAG"

            - name: Package VSIX
              env:
                  CLINE_ENVIRONMENT: production
                  TELEMETRY_SERVICE_API_KEY: ${{ secrets.TELEMETRY_SERVICE_API_KEY }}
                  ERROR_SERVICE_API_KEY: ${{ secrets.ERROR_SERVICE_API_KEY }}
              run: node scripts/package-github-vsix.mjs --out-dir . --verify-install

            - name: Get Previous Tag
              id: prev_tag
              working-directory: ${{ github.workspace }}
              run: |
                  CURRENT_TAG="${{ steps.resolve_tag.outputs.tag }}"
                  PREV_TAG=$(git describe --tags --abbrev=0 "$CURRENT_TAG^" 2>/dev/null || echo "")
                  echo "prev_tag=$PREV_TAG" >> "$GITHUB_OUTPUT"

            - name: Get Changelog Entry
              id: changelog
              working-directory: ${{ github.workspace }}
              run: |
                  CONTENT=$(awk '/^## \[/{if(found) exit; found=1; next} found{print}' CHANGELOG.md)
                  echo "content<<EOF" >> "$GITHUB_OUTPUT"
                  echo "$CONTENT" >> "$GITHUB_OUTPUT"
                  echo "EOF" >> "$GITHUB_OUTPUT"

            - name: Create GitHub Release
              uses: softprops/action-gh-release@v1
              with:
                  tag_name: ${{ steps.resolve_tag.outputs.tag }}
                  files: "apps/vscode/*.vsix"
                  body: |
                      ${{ steps.changelog.outputs.content }}

                      **Full Changelog**: https://github.com/${{ github.repository }}/compare/${{ steps.prev_tag.outputs.prev_tag }}...${{ steps.resolve_tag.outputs.tag }}
                  prerelease: ${{ github.event.inputs.prerelease == 'true' }}
                  draft: ${{ github.event.inputs.draft == 'true' }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
