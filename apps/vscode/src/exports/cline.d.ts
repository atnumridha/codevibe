import type { CodeVibeAPI } from "./codevibe"

/**
 * @deprecated Use CodeVibeAPI from codevibe.d.ts. This alias remains for
 * extension integrations that still compile against the upstream Cline API name.
 */
export type ClineAPI = CodeVibeAPI
